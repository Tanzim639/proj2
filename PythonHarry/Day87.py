# import shutil
# import os

# shutil.copy("main.py", "main2.py") # Copy the file to the same directory
# shutil.copytree(".tutorial", "mytutorial") # Copy the directory and all its contents
# shutil.move(".tutorial/file.file", "file.file") # Move the file to a new location
# shutil.rmtree("mytutorial") # Deletes the entire folder # Deletes the entire folder

# os.remove("file.file") # Deletes the file
# os.rename("file.file", "file2.file") # Renames the file
# os.mkdir("mytutorial") # Creates a folder
# os.rmdir("mytutorial") # Deletes the folder
# os.listdir(".") # Lists the files and folders in the current directory
# os.path.exists("file.file") # returns True for both files and directories.
# os.path.isfile("file.file") # returns True only if the path is a file and not a directory.